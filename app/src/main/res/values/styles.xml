<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- toolbar（actionbar）颜色 -->
        <item name="colorPrimary">#673AB7</item>
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">#512DA8</item>
    </style>

    <style name="loading_small" parent="@android:style/Widget.ProgressBar.Small">
        <item name="android:indeterminateDrawable">@anim/loading</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Dialog对话框 -->
    <style name="MyLoadDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:background">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:gravity">bottom</item>
    </style>

    <!--Dialog Acitivity-->
    <style name="DialogStyle" parent="android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">false</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@color/black_trans</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 模糊 -->
    </style>

    <!--头部textview-->
    <style name="HeaderTextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textColor">@color/head_text_color</item>
        <item name="android:textSize">@dimen/sp18</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="android:gravity">center</item>
    </style>

    <!--输入框项textview -->
    <style name="TextViewStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">@dimen/sp14</item>
        <item name="android:layout_marginLeft">@dimen/dp16</item>
        <item name="android:gravity">center</item>
    </style>
    <!--输入框edittext -->
    <style name="EditTextStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">@dimen/sp14</item>
        <item name="android:textColorHint">@color/gray_color</item>
        <item name="android:background">@null</item>
        <item name="android:layout_marginLeft">90dp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <!--输入框项textview -->
    <style name="CenterTextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/text_color</item>
        <item name="android:textSize">@dimen/sp14</item>
        <item name="android:gravity">center</item>
    </style>

    <!--button -->
    <style name="ButtonStyle">

        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">@drawable/bg_roundbtn_style</item>
        <item name="android:layout_marginLeft">@dimen/dp16</item>
        <item name="android:layout_marginRight">@dimen/dp16</item>
        <item name="android:textColor">@color/head_text_color</item>
        <item name="android:textSize">@dimen/sp17</item>
    </style>

    <!--首页textview-->
    <style name="HomeTextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/color_9966ff</item>
        <item name="android:textSize">@dimen/sp15</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

    <style name="smallStarStyle" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/smallstar_drawable</item>
        <item name="android:numStars">5</item>
        <item name="android:minHeight">@dimen/dp12</item>
        <item name="android:maxHeight">@dimen/dp12</item>
        <item name="android:isIndicator">true</item>
    </style>

    <style name="bigStarStyle" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/bigstar_drawable</item>
        <item name="android:numStars">5</item>
        <item name="android:minHeight">49dp</item>
        <item name="android:maxHeight">49dp</item>
        <item name="android:isIndicator">true</item>
    </style>
</resources>
